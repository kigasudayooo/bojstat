% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{boj_get_layer}
\alias{boj_get_layer}
\title{Retrieve Time-Series Data by Hierarchical Structure (Layer API)}
\usage{
boj_get_layer(
  db,
  frequency,
  layer,
  start = NULL,
  end = NULL,
  lang = c("jp", "en"),
  tidy = TRUE,
  request_interval = 1,
  timeout = 30
)
}
\arguments{
\item{db}{Character. Database name. See \link{boj_db_names}.}

\item{frequency}{Character. Frequency code: \code{"CY"}, \code{"FY"}, \code{"CH"},
\code{"FH"}, \code{"Q"}, \code{"M"}, \code{"W"}, or \code{"D"}. See \link{boj_frequencies}.}

\item{layer}{Character. Layer specification as a comma-separated string
for layer 1 to 5. Wildcard \code{"*"} can be used. Examples:
\itemize{
\item \code{"*"} — all series
\item \code{"1,1"} — layer1 = 1, layer2 = 1
\item \code{"1,*,1"} — layer1 = 1, layer2 = any, layer3 = 1
}}

\item{start}{Character. Start period (see \code{\link[=boj_get_data]{boj_get_data()}} for formats).}

\item{end}{Character. End period.}

\item{lang}{Character. \code{"jp"} or \code{"en"}.}

\item{tidy}{Logical. Return tidy \code{data.frame} (default) or raw list.}

\item{request_interval}{Numeric. Seconds between requests.}

\item{timeout}{Numeric. Request timeout in seconds.}
}
\value{
If \code{tidy = TRUE}, a long-format \code{data.frame}. If \code{FALSE}, raw list.
}
\description{
Fetches data using the hierarchical tree structure of each database,
which is useful when you want all series under a particular category
without knowing series codes in advance.
}
\examples{
\dontrun{
# Balance of payments, monthly, hierarchical slice 1-1-1
df <- boj_get_layer(
  db        = "BP01",
  frequency = "M",
  layer     = "1,1,1",
  start     = "202504",
  end       = "202509"
)
head(df)
}

}
