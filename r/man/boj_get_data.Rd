% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{boj_get_data}
\alias{boj_get_data}
\title{Retrieve Time-Series Data by Series Codes (Code API)}
\usage{
boj_get_data(
  db,
  codes,
  start = NULL,
  end = NULL,
  lang = c("jp", "en"),
  tidy = TRUE,
  request_interval = 1,
  timeout = 30
)
}
\arguments{
\item{db}{Character. Database name (e.g. \code{"FM08"}, \code{"CO"}, \code{"PR01"}).
See \link{boj_db_names} for all available DBs.}

\item{codes}{Character vector of series codes (up to 250 per request).
\strong{Note}: Do NOT include the DB prefix. Use \code{"MADR1Z@D"}, not
\code{"IR01'MADR1Z@D"}. All codes must share the same frequency.}

\item{start}{Character. Start period in the format appropriate for the
series frequency:
\itemize{
\item Monthly / Weekly / Daily: \code{"YYYYMM"} (e.g. \code{"202501"})
\item Quarterly: \code{"YYYYQQ"} (e.g. \code{"202502"} for Q2 2025)
\item Calendar/Fiscal half year: \code{"YYYYHH"} (e.g. \code{"202501"} for first half)
\item Annual: \code{"YYYY"}
}}

\item{end}{Character. End period (same format as \code{start}).}

\item{lang}{Character. Output language: \code{"jp"} (Japanese, default) or \code{"en"}.}

\item{tidy}{Logical. If \code{TRUE} (default), returns a tidy long-format
\code{data.frame}. If \code{FALSE}, returns the raw list from the API.}

\item{request_interval}{Numeric. Seconds to wait before making the request
to avoid overloading the BOJ server. Default is \code{1.0}.}

\item{timeout}{Numeric. Request timeout in seconds. Default is \code{30}.}
}
\value{
If \code{tidy = TRUE}, a \code{data.frame} with columns:
\code{series_code}, \code{name}, \code{unit}, \code{frequency}, \code{category},
\code{last_update}, \code{date}, \code{value}.
If \code{tidy = FALSE}, the raw list from the API response.
}
\description{
Fetches time-series statistical data from the BOJ Time-Series Data Search
by specifying series codes directly.
}
\examples{
\dontrun{
# Overnight call rate (daily)
df <- boj_get_data(
  db    = "FM01",
  codes = c("STRDCLUCON", "STRDCLUCONH", "STRDCLUCONL"),
  start = "202501"
)
head(df)

# Tankan (quarterly)
df <- boj_get_data(
  db    = "CO",
  codes = c("TK99F1000601GCQ01000", "TK99F2000601GCQ01000"),
  start = "202401",
  end   = "202504",
  lang  = "en"
)
}

}
\seealso{
\code{\link[=boj_get_data_all]{boj_get_data_all()}} for automatic pagination,
\code{\link[=boj_get_layer]{boj_get_layer()}} for the Layer API,
\code{\link[=boj_get_metadata]{boj_get_metadata()}} for the Metadata API.
}
