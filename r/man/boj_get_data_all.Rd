% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{boj_get_data_all}
\alias{boj_get_data_all}
\title{Retrieve All Time-Series Data with Automatic Pagination (Code API)}
\usage{
boj_get_data_all(
  db,
  codes,
  start = NULL,
  end = NULL,
  lang = c("jp", "en"),
  request_interval = 1,
  timeout = 30
)
}
\arguments{
\item{db}{Character. Database name (e.g. \code{"FM08"}, \code{"CO"}, \code{"PR01"}).
See \link{boj_db_names} for all available DBs.}

\item{codes}{Character vector of series codes. Unlimited length; the
function splits into chunks of 250 automatically.}

\item{start}{Character. Start period in the format appropriate for the
series frequency:
\itemize{
\item Monthly / Weekly / Daily: \code{"YYYYMM"} (e.g. \code{"202501"})
\item Quarterly: \code{"YYYYQQ"} (e.g. \code{"202502"} for Q2 2025)
\item Calendar/Fiscal half year: \code{"YYYYHH"} (e.g. \code{"202501"} for first half)
\item Annual: \code{"YYYY"}
}}

\item{end}{Character. End period (same format as \code{start}).}

\item{lang}{Character. Output language: \code{"jp"} (Japanese, default) or \code{"en"}.}

\item{request_interval}{Numeric. Seconds to wait before making the request
to avoid overloading the BOJ server. Default is \code{1.0}.}

\item{timeout}{Numeric. Request timeout in seconds. Default is \code{30}.}
}
\value{
A \code{data.frame} (long format) combining all pages.
}
\description{
A wrapper around \code{\link[=boj_get_data]{boj_get_data()}} that automatically handles pagination
when the number of codes exceeds 250 or when the data limit (60,000
observations) is reached.
}
\examples{
\dontrun{
# Fetch more than 250 codes automatically
meta  <- boj_get_metadata("PR01")
codes <- meta$series_code
df    <- boj_get_data_all("PR01", codes)
}

}
